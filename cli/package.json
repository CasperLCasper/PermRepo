{
  "name": "perm-repo",
  "version": "2.0.43",
  "description": "PermRepo CLI — mūžīgs Git repo backups uz Arweave",
  "main": "src/index.js",
  "bin": {
    "perm-repo": "bin/cli.js"
  },
  "scripts": {
    "build": "ncc build src/index.js -o dist"
  },
  "dependencies": {
    "@ardrive/turbo-sdk": "^1.42.0",
    "commander": "^12.1.0",
    "ethers": "^6.13.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
