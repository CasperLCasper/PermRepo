{
  "name": "perm-repo",
  "version": "2.0.27",
  "description": "Mūžīgs Git repo backups uz Arweave — CLI visām CI platformām",
  "main": "src/index.js",
  "bin": {
    "perm-repo": "./bin/cli.js"
  },
  "files": [
    "bin/",
    "src/",
    "web/"
  ],
  "scripts": {
    "build": "esbuild web/js/browser-app.js --bundle --outfile=web/bundle.js --define:global=window"
  },
  "dependencies": {
    "@ardrive/turbo-sdk": "^1.8.0",
    "commander": "^12.0.0",
    "ethers": "^6.13.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
